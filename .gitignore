# llama.cpp clone — install via Homebrew or build from source (see QUICKSTART.md)
llama.cpp/

# Model files — too large for git
models/*.gguf
models/.cache/

# Build output
priorityserve

# macOS
.DS_Store

# Ephemeral load test runs (keep named benchmark results)
results/loadtest_2*.json
